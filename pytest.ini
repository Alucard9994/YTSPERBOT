[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    unit: test unitari — puri, veloci, nessun I/O esterno
    integration: test con DB SQLite reale e FastAPI TestClient
    e2e: test browser end-to-end (playwright, lenti)
